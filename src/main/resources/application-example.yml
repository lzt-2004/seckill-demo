spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_demo?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: your
    password: your
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:  # ← 这里要 2 个空格，和上面的 datasource、jpa 对齐
    redis:
      host: localhost
      port: 6379